packages:
  # Shared is always a code package. Domain packages are dbt Core fallbacks for
  # the public project dependencies declared in dependencies.yml.
  - local: ../shared
  - local: ../platform
  - local: ../supply
  - package: dbt-labs/dbt_utils
    version: 1.3.3
