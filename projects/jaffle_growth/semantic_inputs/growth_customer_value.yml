name: growth_customer_value
grain: customer
owner: growth
description: Lightweight example spec for a customer value segmentation mart.
inputs:
  - ref: fct_customer_value_segments
    join_key: customer_id
  - ref: fct_loyalty_balance_daily
    join_key: customer_id
dimensions:
  - loyalty_region
  - lifecycle_stage
  - value_band
  - care_profile
measures:
  - lifetime_net_revenue_usd
  - lifetime_recipe_margin_usd
  - projected_points_balance

