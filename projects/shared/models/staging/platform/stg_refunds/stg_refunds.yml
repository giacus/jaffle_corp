version: 2

models:
  - name: stg_refunds
    config:
      group: platform
    columns:
      - name: refund_id
        data_tests:
          - not_null
          - unique
    description: "{{ doc('jaffle_platform__stg_refunds') }}"
