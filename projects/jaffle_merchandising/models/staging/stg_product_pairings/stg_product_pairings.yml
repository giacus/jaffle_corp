version: 2

models:
  - name: stg_product_pairings
    config:
      group: merchandising
    columns:
      - name: pairing_id
        data_tests:
          - not_null
          - unique
    description: "{{ doc('jaffle_merchandising__stg_product_pairings') }}"
