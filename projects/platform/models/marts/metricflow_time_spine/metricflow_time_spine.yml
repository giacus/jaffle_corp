version: 2

models:
  - name: metricflow_time_spine
    time_spine:
      standard_granularity_column: date_day
    config:
      group: platform
      meta:
        metricflow_time_spine: true
    columns:
      - name: date_day
        description: "{{ doc('platform__metricflow_time_spine__date_day') }}"
        data_type: date
        granularity: day
        data_tests:
          - not_null
          - unique
    description: "{{ doc('platform__metricflow_time_spine') }}"
