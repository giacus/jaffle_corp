select
    menu_product_window_key,
    publication_id,
    store_id,
    country_code,
    city,
    product_id,
    sku,
    product_name,
    category,
    product_family,
    published_at_utc,
    retired_at_utc,
    menu_section,
    display_rank,
    is_featured,
    published_price_minor,
    currency,
    menu_surface,
    menu_price_band,
    is_limited_time,
    has_perishable_supply,
    updated_at_utc
from {{ ref('int_menu_product_windows') }}
