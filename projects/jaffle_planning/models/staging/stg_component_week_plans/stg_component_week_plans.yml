version: 2

models:
  - name: stg_component_week_plans
    config:
      group: planning
    columns:
      - name: plan_id
        data_tests:
          - not_null
          - unique
      - name: scenario_name
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - base
                  - stretch
    description: "{{ doc('jaffle_planning__stg_component_week_plans') }}"
